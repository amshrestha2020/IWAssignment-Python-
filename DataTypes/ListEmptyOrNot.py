'Program to check a list is empty or not'

class list:
    # a_list = [input("Enter a string or integer:")]
    a_list = []
    if len(a_list) == 0:
        print("List is empty.")
    else:
        print("List is not empty.")
